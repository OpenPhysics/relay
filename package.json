{
  "name": "openphysics-org-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tooling for the OpenPhysics org orchestration repo (Relay): repo metadata helpers and the shared compliance/catalog scripts.",
  "scripts": {
    "test": "bats tests/*.bats"
  },
  "devDependencies": {
    "bats": "^1.13.0",
    "bats-assert": "^2.2.4",
    "bats-support": "^0.3.0"
  }
}
